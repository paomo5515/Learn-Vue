// tuple 元组：多种元素的组合
const info: any[] = ["hello", 18, 1.88]

const name = info[0]

console.log(name.length);

// 元组的特点
const info2: [string, number, number] = ["hello", 18, 1.88]
const name2 = info2[0]
console.log(name2.length);

const age2 = info2[1]
console.log(age2.length);
export { }